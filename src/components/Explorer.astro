---
import { getFolderHref, getPublishedNotes } from '../utils';

interface TreeNode {
  name: string;
  parts: string[];
  folders: Map<string, TreeNode>;
  files: Array<{ title: string; slug: string; date: number }>;
}

const notes = await getPublishedNotes();
const root: TreeNode = { name: '', parts: [], folders: new Map(), files: [] };

for (const note of notes) {
  let current = root;
  for (const segment of note.folderParts) {
    if (!current.folders.has(segment)) {
      const parts = [...current.parts, segment];
      current.folders.set(segment, {
        name: segment,
        parts,
        folders: new Map(),
        files: [],
      });
    }
    current = current.folders.get(segment)!;
  }
  current.files.push({
    title: note.title,
    slug: note.slug,
    date: note.date.getTime(),
  });
}

function escapeHtml(value: string): string {
  return value
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#39;');
}

function renderTree(node: TreeNode): string {
  const buffer: string[] = [];

  const rooms = [...node.folders.values()].sort((a, b) =>
    a.name.localeCompare(b.name, undefined, { numeric: true })
  );

  for (const room of rooms) {
    const href = getFolderHref(room.parts);
    const path = room.parts.join(' › ');
    buffer.push(`
      <details class="folder-group" data-path="${escapeHtml(path)}">
        <summary class="folder-summary">
          <span class="folder-arrow" aria-hidden="true">▾</span>
          <a class="folder-name" href="${escapeHtml(href)}">${escapeHtml(room.name)}</a>
        </summary>
        <div class="folder-children">
          ${renderTree(room)}
        </div>
      </details>
    `);
  }

  const files = [...node.files].sort((a, b) => b.date - a.date || a.title.localeCompare(b.title));
  if (files.length > 0) {
    buffer.push('<ul class="file-list">');
    for (const file of files) {
      buffer.push(`
        <li class="file-item">
          <a href="/${escapeHtml(file.slug)}" class="file-link">${escapeHtml(file.title)}</a>
        </li>
      `);
    }
    buffer.push('</ul>');
  }

  return buffer.join('');
}

const treeHtml = renderTree(root);
---

<nav-explorer class="explorer">
  <div class="explorer-header">Explorer</div>
  <div class="explorer-tree" set:html={treeHtml} />
</nav-explorer>

<script>
  class NavExplorer extends HTMLElement {
    private KEY = 'open-folders';

    connectedCallback() {
      this.restoreState();

      this.addEventListener('toggle', (e) => {
        const el = e.target as HTMLDetailsElement;
        const path = el.dataset.path;
        if (path) this.saveState(path, el.open);
      }, true);

      this.addEventListener('click', (e) => {
        const target = e.target as HTMLElement;
        if (target.closest('.folder-name')) e.stopPropagation();
      });
    }

    private restoreState() {
      try {
        const saved = localStorage.getItem(this.KEY);
        if (!saved) return;

        const openFolders = JSON.parse(saved) as string[];
        if (!Array.isArray(openFolders)) return;

        for (const path of openFolders) {
          const folder = this.querySelector(`details[data-path="${CSS.escape(path)}"]`);
          if (folder instanceof HTMLDetailsElement) folder.open = true;
        }
      } catch {
        localStorage.removeItem(this.KEY);
      }
    }

    private saveState(path: string, isOpen: boolean) {
      if (!path) return;
      let openFolders: string[] = [];
      try {
        openFolders = JSON.parse(localStorage.getItem(this.KEY) || '[]');
        if (!Array.isArray(openFolders)) openFolders = [];
      } catch {
        openFolders = [];
      }

      const next = new Set(openFolders);
      if (isOpen) next.add(path);
      else next.delete(path);
      localStorage.setItem(this.KEY, JSON.stringify([...next]));
    }
  }

  if (!customElements.get('nav-explorer')) {
    customElements.define('nav-explorer', NavExplorer);
  }
</script>

<style is:global>
  .explorer-header {
    font-size: 0.8rem;
    font-weight: 700;
    color: #64748b;
    letter-spacing: 0.02em;
    margin-bottom: 0.5rem;
  }

  .folder-group {
    margin-bottom: 0.15rem;
  }

  .folder-children {
    padding-left: 0.75rem;
    margin-left: 0.45rem;
    border-left: 1.5px solid #e2e8f0;
  }

  .folder-summary {
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.2rem 0.3rem;
    border-radius: 4px;
    color: #334155;
    font-weight: 500;
    list-style: none;
    user-select: none;
  }

  .folder-summary::-webkit-details-marker {
    display: none;
  }

  .folder-summary:hover {
    background: #f8fafc;
  }

  .folder-arrow {
    display: inline-block;
    font-size: 0.75rem;
    color: #94a3b8;
    width: 12px;
    text-align: center;
    flex: 0 0 12px;
  }

  details:not([open]) > .folder-summary .folder-arrow {
    transform: rotate(-90deg);
  }

  .folder-name {
    color: inherit;
    text-decoration: none;
    min-width: 0;
    overflow-wrap: anywhere;
  }

  .folder-name:hover {
    text-decoration: underline;
  }

  .file-list {
    list-style: none;
    padding: 0;
    margin: 0.15rem 0 0.35rem;
  }

  .file-link {
    display: block;
    padding: 0.2rem 0.4rem;
    border-radius: 4px;
    color: #64748b;
    text-decoration: none;
    line-height: 1.4;
    overflow-wrap: anywhere;
  }

  .file-link:hover {
    background: #f0f9ff;
    color: #0369a1;
  }
</style>
